import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#F22323'
        },
        secondary:{
            main: '#FFFFFF'
        },
        black:{
            main: '#0000'
        }
    }
});

export default theme;
