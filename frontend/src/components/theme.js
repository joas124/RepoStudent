import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#F22323'
        },
        secondary:{
            main: '#FFFFFF'
        }
    }
});

export default theme;
